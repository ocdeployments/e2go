# SPRINT J-1 — Case Intelligence Core (The CPU)

**Created:** Session 91 — June 30, 2026. **Branch:** dev.
**Recovery doc:** If a session crashes mid-build, resume from the first unchecked task. Each task is independently shippable and verifiable.
**Full architecture:** see `docs/COMPREHENSION_ENGINE_PLAN.md`.

---

## LOCKED DECISIONS (owner-confirmed, Session 91)

| # | Decision | Choice |
|---|---|---|
| 1 | Orchestrator authority | **Gating** — certification gates phases; CPU can reject output and force re-runs (with max-retry + escalate-to-human guard) |
| 2 | KB grounding scope | **Full 177-doc corpus** — chunk + embed everything in `docs/` |
| 3 | First build target | **CIC-0 + CIC-1** — grounding + read-only brain (no orchestration until reasoning proven) |
| 4 | Comprehension model | `xiaomi/mimo-v2.5-pro` (OpenRouter) |
| 5 | FDD path | Auto-seed `fdd_analyses` from comprehension (kills two-pipeline divergence) |
| 6 | Document conflicts | Non-blocking, flag-for-review |

**Hard boundaries (never violate):**
- Numbers stay deterministic (gap/FDD/eligibility scores + dollar figures are pure-TS/extracted). CPU frames figures, never invents them.
- Provenance on every Case Model fact (quiz / answer / doc id / KB chunk).
- The verifier MUST be able to reject — tested with a known-bad input.
- Every strategic claim cites a KB chunk or a deterministic score.
- ANTHROPIC_API_KEY only in generation-engine.ts + /api/fdd/*. CPU/comprehension uses OpenRouter (callLLM).

---

## BUILD SEQUENCE & PROGRESS

Status key: ⬜ not started · 🔶 in progress · ✅ done · ⛔ blocked

### CIC-0 — Grounding (make the doctrine retrievable)
- ✅ **0.1** Migration: `case_model`, `case_theory`, `document_intelligence` tables + widen `uploaded_documents.doc_type` CHECK (6→11 types incl. new Session-91 types). Idempotent, RLS on all. → `supabase/migrations/20260630100000_case_intelligence_core.sql` (also creates `kb_chunks` + `match_kb` RPC).
- ✅ **0.2** KB chunker script → `scripts/seed-kb-corpus.ts`: walks all knowledge `docs/*.md` (excludes build/meta via EXCLUDE_PREFIXES), heading-aware chunk (~600 tok, 100 overlap), embeds via `text-embedding-3-small`, upserts to `kb_chunks`. Idempotent (sha256 `content_hash` skip-unchanged + orphan prune). Dimension auto-tagged by filename.
- ✅ **0.3** `match_kb(query_embedding, match_count, filter_dimension)` RPC (in migration) + typed `retrieveDoctrine(query, {dimension?, matchCount?, minSimilarity?})` + `formatDoctrineForPrompt()` helper → `src/lib/doctrine-retrieval.ts`.
- ✅ **0.4** Verify: applied migration → ran chunker (949 chunks / 93 docs) → spot-checked retrieval on 3 queries (denial D-07, crypto SoF, marginality rebuttal) — all returned correctly-grounded, high-similarity doctrine hits (sim 0.45–0.74). Found + fixed two real bugs along the way: (1) `doctrine-retrieval.ts` was missing the Node<22 WebSocket polyfill present in `seed-kb-corpus.ts`, causing `createClient()` to throw and `retrieveDoctrine()`'s catch-block to silently degrade to `[]` on every call; (2) `kb_chunks_embedding_idx` (ivfflat) was created in the migration before any rows existed, producing degenerate clusters that returned 0 rows on index-scan queries regardless of the embedding — fixed via `REINDEX INDEX kb_chunks_embedding_idx;` after seeding. Also added `DROP POLICY/TRIGGER IF EXISTS` guards to the migration (Postgres `CREATE POLICY`/`CREATE TRIGGER` have no native `IF NOT EXISTS`).

### CIC-1 — Read-only brain (Perceive + Ground + Reason)
- ✅ **1.1** Document channel — `document-comprehension-engine.ts`: Stage 1 comprehend (memo + claims, mimo-pro) + Stage 2 reconcile (ledger, SOURCE_PRIORITY, conflicts queue) → writes `document_intelligence`. Triggered after parse-document succeeds (wiring fixed Session 91 cont. — was imported but never called; now sequenced before buildCaseIntelligence in `src/app/api/apply/parse-document/route.ts`).
- ✅ **1.2** `case-intelligence-core.ts` — Faculty 1 PERCEIVE: `assembleCaseModel()` assembles `case_model` from quiz + applications + answers + document ledger + followup + simulator_sessions + case_briefs, each fact with provenance/confidence. Computes `data_state`. Upserts on `application_id`.
- ✅ **1.3** Faculty 2 GROUND + Faculty 3 REASON: `generateCaseTheory()` retrieves doctrine per populated dimension via `retrieveDoctrine()`, then reasons as a five-expert panel (immigration consultant, E-2 consular officer, immigration attorney, franchise development consultant, market analyst — owner directive) to produce `case_theory` (narrative, transferable-skills map, numbers strategy, per-dimension verdicts with **creative, concrete gap-fill suggestions per expert hat** — not just status labels, directive list for peripheral engines). Output stored; NOT yet wired to engines (CIC-2 gate still applies). Handles empty-KB gracefully (no doctrine retrieved yet, since 0.4 is pending) by instructing the model not to assert uncited legal standards.
- ✅ **1.4** Surface the Case Theory read-only on `/case-profile` (new section) so reasoning quality is visible and reviewable. → `caseTheory` added to `/api/dashboard/case-profile` response (`CaseTheoryUI` type) + rendered in `CaseProfilePage.tsx`'s existing "04 Case Intelligence" section via a new `CaseTheoryBlock` component (narrative, numbers strategy, transferable skills, per-dimension verdicts with persona-tagged gap-fill suggestions). Conditionally rendered (`{data.caseTheory && ...}`) — verified graceful no-row fallback against a live unpaid test account (`/api/dashboard/case-profile` → 200, `caseTheory: null`, zero console errors).
- ✅ **1.5** Trigger wiring: `buildCaseIntelligence(applicationId, userId)` fires fire-and-forget after document parse (sequenced after comprehension), after answer save (`/api/answers`), and after simulator outcome save (`/api/simulator/outcome`, guarded on optional `applicationId`) — mirrors the `buildCaseProfile` pattern.
- ✅ **1.6** Verified on all three seeded test accounts (Canada/France/UK) via `scripts/cic16-verify.ts`. All three produce `status: complete` with full 9-dimension coverage, 5-7 directives, and correctly-cited doctrine. Narratives are specific and accurate (correct franchise names, dollar figures, treaty basis, denial-risk framing per account). **Found + fixed the REASON model problem along the way**: owner-locked `xiaomi/mimo-v2.5-pro` (and `xiaomi/mimo-v2.5`) never produced output on the real production prompt — both are reasoning models that consume their entire `max_tokens` budget on hidden reasoning tokens (confirmed via raw OpenRouter `completion_tokens_details.reasoning_tokens`), leaving `content: undefined`, or with a larger budget, never converging at all (still incomplete past 110s). Tested `z-ai/glm-5.2` per owner's instruction — works for small prompts (~7K chars) but exhibits the same runaway-reasoning behavior on large prompts (35K-56K chars, i.e. accounts with `case_brief` populated). **Switched `OPENROUTER_MODELS.extract` to `['google/gemini-2.5-pro', 'z-ai/glm-5.2']`** (gemini primary — reliably handles large contexts without runaway reasoning; glm-5.2 fallback for cost). Also reduced doctrine `matchCount` from 4→2 per dimension (prompt-size discipline) and added JSON-repair logic to `parseCaseTheory()` (closes unterminated braces/strings) so a token-ceiling truncation degrades gracefully instead of failing the whole Case Theory. `max_tokens` raised to 8000 — gemini doesn't reasoning-runaway at this budget, so full `dimension_verdicts` is no longer truncated. **This is a change to owner-locked decision #4** (was `xiaomi/mimo-v2.5-pro`) — flagging for explicit sign-off since the original lock predates this evidence. Re-verified on a clean run after the fix landed (and again after the fingerprint-skip change below): all three accounts `status: complete`, 8-9/9 dims, 6-7 directives.

**Cost finding + fix (same session):** pulled real `llm_cost_log` rows for `task=extract` — average $0.07/call (gemini-2.5-pro output runs 5-8K tokens regardless of input size, since it writes a full 9-dimension verdict every time). `buildCaseIntelligence()` fires fire-and-forget on three triggers per applicant (doc parse, answer save, simulator outcome), so realistic per-applicant cost was $0.35-0.70+, not $0.07 — and a no-op re-save would re-cost a full call. Fixed by adding `source_fingerprint` (sha256 of the Case Model facts that feed the prompt, via additive migration `20260630110000_case_theory_fingerprint.sql`, applied) to `case_theory`; `generateCaseTheory()` now hashes the fact set first and returns the existing theory with zero LLM calls if the fingerprint is unchanged. Verified: ran `cic16-verify.ts` twice back-to-back — pass 1 regenerated (new fingerprint stored), pass 2 completed all three accounts in 7s total with zero new `llm_cost_log` rows.

### CIC-2 — First orchestration (generation) — *after CIC-1 sign-off*
- ✅ **2.1** Case Theory strategic brief injected into generation payload. `buildCaseTheoryBrief()` in `generation-engine.ts` formats narrative backbone + numbers strategy + per-dimension verdicts (filtered to doc-type-relevant dimensions via `DOC_TYPE_DIMENSIONS`) + `engine:'generation'` directives as the first block in `callClaudeAPI`'s `userMessage`. `buildGenerationPayload()` fetches `case_theory` row and adds `case_theory_brief?: string` to `GenerationPayload` (`src/types/generation.ts`). Graceful on miss (brief omitted for sparse accounts with no case_theory yet). Verified via spot-check script: UK test account cover_letter payload includes full 60-line strategic brief with narrative, 3 numbers, 8 dimension verdicts with expert gap-fill suggestions, 2 targeted directives. Build clean.
- ✅ **2.2** Verifier pass built → `src/lib/cic-verifier.ts`. `verifyCaseTheoryCompliance()` runs after generation, before client sees draft. Gets: Case Theory directives (filtered to doc-type-relevant dimensions via `DOC_TYPE_DIMENSIONS`) + dimension verdicts + draft text (first 12K chars). Outputs `VerifierResult`: `{ overall: pass|fail|pass_with_notes, directivesFollowed[], figuresCorrect, figureIssues[], contradictions[], repetitionIssues[], toneIssues[], notes[], correctionBrief }`. `correctionBrief` is formatted corrective feedback ready to prepend to the retry generation call. Null on no case_theory (sparse accounts) or verifier LLM failure — both treated as pass so client is never blocked. Uses `callLLM task:'extract'` (gemini-2.5-pro primary). Migration `20260630120000_generated_documents_cic.sql` adds `verifier_result jsonb`, `verifier_attempts int`, `client_certified bool`, `certified_at`, `locked_passages jsonb` to `generated_documents` — apply in Supabase SQL editor. Build clean.
- ✅ **2.3** Retry loop wired in `runGenerationPipeline()` (`generation-engine.ts`): verifier runs up to 3 times per document; on `fail`, `correctionBrief` is prepended to `case_theory_brief` and `callClaudeAPI` is called again with the corrective payload. Final `content` + `verifier_result` + `verifier_attempts` written to `generated_documents` before client sees anything. Client certification loop (UI + `client_certified` / `locked_passages` fields) → **CIC-P.4** (package assembly gate). Migration `20260630120000_generated_documents_cic.sql` must be applied: `ALTER TABLE generated_documents ADD COLUMN IF NOT EXISTS ...` (see migration file). Verifier `pass` or `pass_with_notes` → document enters client review state on `/documents`. Client can: (1) read and certify as-is, (2) edit any passage inline (edits locked — survive future regeneration), (3) request full regeneration with an optional note (note feeds next generation call). Verifier `fail` → auto-retry generation with failure reasons as corrective feedback; max 3 retries, then surfaces to client as "needs your input" rather than escalating to internal team. Package assembly (CIC-P.4) only includes certified documents. Figure consistency guard: if client edits a figure that contradicts case_model, warning shown (not hard block). `generated_documents` schema: add `client_certified bool`, `certified_at`, `locked_passages jsonb`.

### CIC-P — Package Orchestration — *after CIC-2*
- ✅ **P.1** Package Manifest built → `src/lib/cic-package-manifest.ts` + `GET /api/dashboard/package-manifest?applicationId=`. `buildPackageManifest()` merges `generated_documents` (status/certified/verifier_result), `uploaded_documents` (doc_type/file_name), and answer flags for conditional docs (spouse/property_portfolio) into a numbered tab list. 22 tab templates in canonical E-2 order (cover page → index → generated docs → client-provided exhibits). Each tab: `{ tabNumber, label, description, source, status, documentType?, uploadedDocType?, clientCertified?, verifierOverall? }`. Status: certified | awaiting_client | generating | draft | uploaded | outstanding. `packageReady: true` when zero outstanding and all generated tabs certified. Verified on UK test account: 12 tabs correct, all outstanding (no generation run yet — expected). Build clean.
- ✅ **P.2** Cross-document consistency sweep built → `src/lib/cic-consistency-sweep.ts` + `GET /api/dashboard/consistency-sweep?applicationId=`. **Two-phase architecture**: Phase 1 (no LLM) — 8 regex extraction rules (investment_amount, job_creation_count, treaty_nationality, applicant_name, business_name, franchise_brand, net_worth, consulate_post) extract field values from each generated document, compare against canonical values from `case_theory.numbers_strategy`; flags with severity `critical` (figures/treaty/names) or `warning` (dates/roles). Phase 2 (one LLM call, gemini) — compact opening excerpts (~500 chars each) from all generated docs sent alongside `case_theory.narrative`; semantic audit checks for contradictory characterizations of the investment, management role framing, business-stage description, or any passage undermining the narrative backbone. Result type: `{ passed, canonicalValues[], phase1Issues[], phase2SemanticIssues[], summary }`. Stored on `document_generation_jobs.consistency_result` (jsonb — migration `20260630130000_generation_jobs_consistency.sql`, apply in Supabase SQL editor). Wired into `runGenerationPipeline()` at quality step 2, replacing old regex-only `checkConsistency()` call. Old `checkConsistency` export preserved for backwards compat. Build clean, tsc clean.
- ✅ **P.3** Intra-document flow directives built into the verifier (`cic-verifier.ts`). Zero extra LLM cost — added to the existing verifier call. **`DOC_SECTION_CONTRACTS`**: per-document section-level structural contracts for 5 doc types (cover_letter, business_plan, source_of_funds, qualifications, marginality_rebuttal) — each section defines what it MUST establish and what it must NOT repeat from a prior section, enforcing single-responsibility across sections. **Argument density rule**: every body paragraph must carry (a) a qualifying fact, (b) a denial-risk rebuttal, or (c) an exhibit reference — filler paragraphs flagged as flow issues. New `flow_issues[]` field added to `VerifierResult` and surfaces in `correctionBrief` on retry with instruction to follow the required section order. `buildVerifierPrompt()` extended with `sectionContractBlock` injected between directives and document text. `parseVerifierResult()` parses `flow_issues` from JSON response. Flow issues that indicate a required section is missing → `overall: fail` (triggers regeneration). Minor flow / density issues → `pass_with_notes`. Build clean, tsc clean.
- ✅ **P.4** Package assembly gate + client certification + regeneration request built. **ZIP assembly** infrastructure was already complete (cover page, TOC, tab dividers, per-document .docx, checklist — all in `/api/generate/download/[applicationId]`). CIC-P.4 added: (1) **CIC completeness gate** — replaced old `applicant_acknowledged + final_status=RELEASED` check with `buildPackageManifest().packageReady`; gate returns structured error with `reasons[]`, certified/outstanding counts. (2) **`POST /api/dashboard/certify-document`** — client certifies a generated document; sets `client_certified=true`, `certified_at`, merges `locked_passages[]` (inline edits that survive future regeneration). (3) **`POST /api/dashboard/request-regeneration`** — client requests regen with optional note; clears `client_certified`, stores `client_regen_note` on the document row, inserts a new `document_generation_jobs` row queued for the single document. (4) **Client note injection** — pipeline reads `client_regen_note` from the job row at startup; prepends a `CLIENT REVISION REQUEST` block to `case_theory_brief` so the generation engine knows exactly what the client found wrong. Migration `20260630140000_cic_p4_package_assembly.sql`: adds `client_regen_note text` to `generated_documents` and `document_generation_jobs` — apply in Supabase SQL editor. Build clean, tsc clean.
- ✅ **P.5** Change impact tracking built → `src/lib/cic-change-impact.ts` + `GET|DELETE /api/dashboard/change-impact?applicationId=`. Full flow: (1) `buildCaseIntelligence()` extended with optional `triggeredByDocType` param; when set, it snapshots `dimension_verdicts` from the DB **before** re-reasoning, then compares against the new verdicts after; (2) `computeChangeImpact()` diffs old vs new verdicts across all 9 dimensions, identifies which changed, maps each changed dimension to the generated documents that cover it (via `DOC_TYPE_DIMENSIONS`), filters to only reviewable docs (awaiting_client / certified — not already queued), and scores urgency (high = source_of_funds/investment/business/franchise changed; medium = verdict shifted to/from proven/contradicted; low = other). (3) `ChangeImpactReport` stored on `case_theory.impact_report` (jsonb — migration `20260630150000_case_theory_impact_report.sql`, apply in Supabase SQL editor). (4) Parse-document route passes `resolvedDocType` as `triggeredByDocType` to `buildCaseIntelligence()`. (5) API: GET returns `{ impactReport }` (null when none); DELETE dismisses (sets `impact_report: null`) after client has reviewed. Client UI reads the report to surface: "Your new FDD changed 2 dimensions. Cover Letter and Business Plan need regeneration because: source_of_funds: evidence strengthened (weak → proven)." Build clean, tsc clean.

### CIC-3 — Expand peripherals — *later*
- ✅ **3.1** Gap analysis now folds in the CPU's comprehension. `gap-analysis-engine.ts` stays pure/deterministic — `scoreCase()` gained a 7th optional `cpuContext?: CpuGapContext` param; `applyCpuContext()` runs AFTER all scoring (never recomputes a score or risk level) and does two descriptive things: (1) unshifts substantive **ledger facts** (`label: value`) onto the matching gap category's `evidence[]` via `DIMENSION_TO_GAP_CATEGORY` (so the LLM cites doc-derived evidence instead of re-asking), (2) prefixes `⚑` + a risk note onto any `denialFactor` whose code is in `activeDenialCodes`. `generation-engine.ts`: `buildGenerationPayload()` now fetches `document_intelligence.ledger` + `case_theory` up front (single `Promise.all`), `buildCpuGapContext()` maps ledger→`LedgerFact[]` and derives `activeDenialCodes` from the case theory's unproven dimension verdicts (status weak/missing/contradicted → `DIMENSION_DENIAL_CODES`); `buildGapContext()` surfaces an **ACTIVE DENIAL RISKS** block (CPU-flagged, doc-relevant codes only) + an **Evidence on file** line per weak category. Returns `undefined` for sparse accounts → generation unchanged. tsc clean.
- ✅ **3.2** FDD auto-seed from comprehension. New `src/lib/cic-fdd-seed.ts` → `seedFddAnalysisFromUpload()`. When `/api/apply/parse-document` resolves an import as `doc_type='fdd'` (PDF, with an `applicationId`), it now persists the PDF to the same `application-documents` bucket the dedicated `/api/fdd/upload` route uses and seeds a `pending` `fdd_analyses` row (transaction_type `new_unit`, `target_state` pulled from the application) — so the full FDD Intelligence pipeline (extract→score→territory→questions→report) lights up with **no re-upload**. Boundaries held: only FDDs are persisted (every other import keeps `file_path=''`, per "never store sensitive docs" — an FDD is the franchisor's disclosure doc, which the FDD product already stores); seeded `pending` only (no auto-run of the paid LLM extraction); idempotent on `(application_id, original_filename)` with orphaned-storage rollback on insert failure; fully non-blocking fire-and-forget so the Import Hub response is never delayed. tsc clean.
- ✅ **3.3** Simulator-prep dossier now consumes the Case Theory. `/api/simulator/prep-kit` already overlaid territory (FDD `territory_analysis`) + market (`QMA-*` answers) data; CIC-3.3 adds the CPU's steer on top: a 6th parallel fetch pulls `case_theory.dimension_verdicts` + `directives`; `cpuWeakDimensions` (verdicts weak/missing/contradicted) and `cpuPriorityDirectives` (directives tagged `engine='simulator_prep'` OR aimed at a weak dimension) flow into `caseContext`. Probe selection is now CPU-driven — `CPU_DIM_TO_WP` force-includes the interview probe that pressure-tests each weak dimension (source_of_funds→WP-03, investment→WP-01, operations→WP-02, background→WP-04), unioned with the legacy score triggers. New dossier rule makes the LLM lead section 3 (risk register) + section 6 (revision focus) with the CPU's weak dimensions and weave each directive's instruction into the section-7 answer frameworks. All additive/descriptive — no numbers derived. tsc clean.

### CIC-4 — Certified readiness — *later*
- ✅ **4.1** `/documents/[applicationId]` rebuilt around the CIC certification flow. **What changed vs. the old page:** (1) Three parallel fetches on load: `generate/documents`, `dashboard/package-manifest`, and `dashboard/change-impact`. (2) **Change-impact banner** at top — high/medium/low urgency, names the affected documents, dismisses via DELETE. (3) **Package progress strip** — certified/awaiting counts, live progress bar, "Package ready" / "Certification in progress" pill. (4) Old `approveDocument()` → old `status='approved'` PATCH replaced by `certifyDocument()` → POST `/api/dashboard/certify-document`; document cards now show "CERTIFIED" (green) vs. "AWAITING CERTIFICATION" (amber) badges. (5) "Revise" button replaced by "Request New Draft" → POST `/api/dashboard/request-regeneration` with optional client note. (6) Download gate condition changed from `allApproved && allAcknowledged` to `manifest.packageReady && allAcknowledged` — so the download is gated on the CIC manifest (not just old status field). (7) Outstanding items list (tabs with `status='outstanding'` from manifest). Backward-compat: `select('*')` already returns `client_certified` from the documents API. tsc clean, build clean.

### CIC-5 — Cross-client learning ("genuine learning from experience") — *greenlit Session 93, gated on outcome capture*
- ⬜ **5.1** Outcome-capture table: `application_id`, outcome enum (approved/denied/rfe/pending), consulate, `denial_codes[]`, officer_probes, decisive_dimensions, timeline, consent flag. Additive migration. (Decision **D4** — recommend build now.)
- ⬜ **5.2** Client outcome survey delivered via `email-scheduler` clock2 (post-outcome). Question set **needs owner domain input** (Decision **D5**).
- ⬜ **5.3** Anonymization + consent model (Decision **D6**, privacy gate) — strip PII, generalize figures to bands, drop re-identifying free-text; consent basis (terms update / opt-in) settled before any real data enters a shared corpus.
- ⬜ **5.4** Anonymized, outcome-indexed **case library** as a SECOND RAG corpus alongside `kb_chunks` (which is authored doctrine, not learned-from-outcomes). Retrieved at reason-time: "clients like this, at this consulate, with this profile, were approved/denied because…". Honest limit: advisory-only until N is statistically meaningful — never deterministic.

> **Decision register — resolved 2026-06-30 (Session 94):**
> - **D1 ✅ merge** — phase 1 done: migration `20260630170000_case_model_d1_merge.sql` adds archetype/score columns to `case_model`; `buildCaseProfile()` now syncs them after writing `case_profiles` (backward-compat sync). Phase 2 (drop `case_profiles` writes + migrate ~14 readers to `case_model`) is a follow-on sprint.
> - **D2 ✅ delete** — `src/lib/quiz-scoring.ts` deleted (zero callers confirmed).
> - **D3 ✅** — decision noted: `assembleCaseModel()` is the canonical upstream; `case_profiles` is deprecated-in-place (writes still happen for reader compat; reads should migrate to `case_model/case_theory`).
> - **D4 ✅ build now** — `application_outcomes` table + RLS + `/api/dashboard/outcome` (GET/POST upsert). Migration `20260630160000_outcome_capture.sql`. Apply in Supabase SQL editor.
> - **D5 ⏳ pending** — survey question design needs owner input (what questions to ask post-outcome). Will design question set when Romy provides domain guidance.
> - **D6 ✅ three-point consent** — (1) signup checkbox [pending], (2) terms-update prompt on next login [pending], (3) before-download checkbox in `/documents` gate [built — `acknowledgments.outcomes_consent`]. `application_outcomes.consent_given` only set true when client explicitly ticks. No data enters CIC-5.4 corpus without `consent_given=true` AND `anonymized_at` set (CIC-5.3 not yet built).

---

## SESSION LOG (append per work block; survives crash)

- **2026-06-30 — Session 91:** Architecture designed + reframed to CPU. Decisions 1–6 locked. Sprint doc created.
- **2026-06-30 — Session 91 (cont.):** CIC-0 grounding built. **0.1** migration `20260630100000_case_intelligence_core.sql` (4 tables + doc_type fix + `match_kb` RPC, all RLS/idempotent). **0.2** chunker `scripts/seed-kb-corpus.ts` (full corpus, hash-idempotent, orphan prune, dimension tagging; excludes build/meta docs — judgment call, EXCLUDE_PREFIXES editable). **0.3** `src/lib/doctrine-retrieval.ts` — `retrieveDoctrine()` + `formatDoctrineForPrompt()`. **Next: 0.4** — owner applies migration + runs chunker, then spot-check retrieval. Then CIC-1.1 (document channel).
- **2026-06-30 — Session 93:** Architecture reconciliation, **no code changed** (option 4 holds). Re-read `docs/FEATURE_INVENTORY.html` against live code and corrected an earlier "engines are siloed / generation is blind" overstatement: the flat `answers` table is already a working cross-engine bus (FDD writeback `source=fdd_intelligence`, market QMA-* keys, simulator coaching, voice profile — all read by generation via `.select('*')`). Genuine open gaps confirmed: `case_theory` consumed only by the dashboard, generation's degraded `scoreCase()` gap context (empty docs/brief/sim at `generation-engine.ts:494`), `case_profiles`/`case_model` parallelism, two doc pipelines, orphaned `quiz-scoring.ts`. **CIC-2 reframed** as "upgrade the flat bus to structured `case_model`/`case_theory`," not "build missing wiring." Added **CIC-5** (cross-client learning, greenlit) and the **D1–D6 decision register**. Updated inventory gaps/engines/silos/flows + `BUILD_TRACKER.md` to match. Next: owner decisions D1–D6 + CIC-0.4.
- **2026-06-30 — Session 94:** Picked up after the parallel session finished the whole CIC-P track (P.1–P.5 ✅). **Completed the entire CIC-3 track (3.1–3.3) — gap analysis + FDD + simulator integration.** **3.1** gap engine now consumes the CPU's comprehension while staying pure/deterministic: `gap-analysis-engine.ts` gained `LedgerFact`/`CpuGapContext`, `DIMENSION_TO_GAP_CATEGORY`, `applyCpuContext()` (runs at END of `scoreCase()`, 7th optional param — only enriches descriptive output, never recomputes a score/risk); `generation-engine.ts` `buildGenerationPayload()` fetches `document_intelligence.ledger` + `case_theory` in one `Promise.all`, `buildCpuGapContext()` derives `activeDenialCodes` from unproven verdicts, `buildGapContext()` emits ACTIVE DENIAL RISKS + Evidence-on-file. **3.2** new `src/lib/cic-fdd-seed.ts` — when parse-document resolves an FDD it persists the PDF + seeds a `pending` `fdd_analyses` row (idempotent, rollback-safe, non-blocking) so the FDD product lights up with no re-upload; only FDDs are persisted (every other import stays `file_path=''`). **3.3** `/api/simulator/prep-kit` adds a 6th fetch for `case_theory`; `cpuWeakDimensions` + `cpuPriorityDirectives` steer the dossier, and `CPU_DIM_TO_WP` makes interview-probe selection CPU-driven (unioned with legacy score triggers). `npx tsc --noEmit` clean; **`npm run build` clean** (all routes compiled). Working tree still uncommitted (whole CIC sprint). **Next:** CIC-4.1 (package-level go-ahead gate on `/documents` — note P.4 already built `buildPackageManifest().packageReady` gating the download route; 4.1 is the **UI** surface of that on the documents page, so it pulls in the global UI build process). Then CIC-5 is gated on owner decisions D4/D5/D6.
- **2026-06-30 — Session 92:** CIC-1 brain built end-to-end. Found and fixed the **1.1 wiring gap**: `comprehendApplicationDocuments` had been imported into `parse-document/route.ts` but never called — now sequenced fire-and-forget after `extracted_json` persists. Wrote `src/lib/case-intelligence-core.ts`: **1.2** `assembleCaseModel()` (Faculty 1 PERCEIVE, reads 7 source tables into a provenance-tagged Case Model) and **1.3** `generateCaseTheory()` (Faculty 2 GROUND via per-dimension `retrieveDoctrine()` calls + Faculty 3 REASON). Per owner's explicit instruction, REASON now runs as a **five-expert panel** (senior immigration consultant, senior E-2 consular officer, senior immigration attorney, senior franchise development consultant, senior market analyst) synthesized into one Case Theory — the system prompt requires creative, concrete, honest gap-fill suggestions per dimension (not just "missing" labels), tagged by which expert hat proposed them. Hard boundaries preserved: numbers never invented (only Case Model facts selected/framed), every claim traceable to a fact or KB citation, graceful no-uncited-doctrine fallback while the KB is unseeded. **1.5** trigger wiring added to `/api/apply/parse-document`, `/api/answers`, `/api/simulator/outcome`. **1.4** Case Theory surfaced read-only on `/case-profile` (`CaseTheoryUI` type on the dashboard API + `CaseTheoryBlock` component, persona-tagged gap-fill suggestions rendered per dimension). `npx tsc --noEmit` and `npm run build` both clean; live-verified the API contract against a real (unpaid, unseeded) test account — 200 OK, `caseTheory: null`, zero console errors, correctly gated behind the `/case-profile` payment middleware. **CIC-1 is now code-complete (1.1–1.5).** **Next:** 0.4 (owner: apply migration + set `OPENAI_API_KEY` + run `npx tsx scripts/seed-kb-corpus.ts`) unblocks 1.6 (verification gate on a seeded test account before CIC-2 may begin).

---

## RESUME CHECKLIST (read this first after a crash)
1. `git status` + `git log --oneline -5` — see what landed.
2. Read the SESSION LOG above for the last completed block.
3. Find the first ⬜/🔶 task in BUILD SEQUENCE — resume there.
4. Confirm no migration was half-applied (check Supabase + `supabase/migrations/`).
5. `npm run build` must be clean before continuing.
